<% if @view_object.errors[:old_password].empty? %>
  <%= turbo_stream.update :old_password %>
<% end %>

<% if @view_object.old_password.present? && @view_object.errors[:old_password].present? %>
  <%= turbo_stream.update :old_password do %>
   <%= @view_object.errors[:old_password].join(", ") %>
  <% end %>
<% end %>

<% if @view_object.errors[:new_password].empty? %>
  <%= turbo_stream.update :new_password %>
<% end %>

<% if @view_object.new_password.present? && @view_object.errors[:new_password].present? %>
  <%= turbo_stream.update :new_password do %>
   <%= @view_object.errors[:new_password].join(", ") %>
  <% end %>
<% end %>

<% if @view_object.errors[:new_password_confirmation].empty? %>
  <%= turbo_stream.update :new_password_confirmation %>
<% end %>

<% if @view_object.new_password_confirmation.present? && @view_object.errors[:new_password_confirmation].present? %>
  <%= turbo_stream.update :new_password_confirmation do %>
   <%= @view_object.errors[:new_password_confirmation].join(", ") %>
  <% end %>
<% end %>
